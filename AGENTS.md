# AGENTS.md

See **[CLAUDE.md](CLAUDE.md)** for the full working context: what this app is, the
hard constraints behind its architecture, how to add a tool, the settings, theme
and font systems, the performance model and how to re-measure it, the release
process, and the norms expected of changes here.

That file is the single source of truth for every agent, Claude or otherwise —
this pointer exists so tools that look for `AGENTS.md` find it. Duplicating the
content here would only let the two copies drift apart.
